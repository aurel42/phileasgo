import { adjustFont as adjustFontUtil } from './mapUtils';
import { getFontFromClass as getFontFromClassUtil, measureText as measureTextUtil } from '../metrics/text';

export const adjustFont = adjustFontUtil;
export const getFontFromClass = getFontFromClassUtil;
export const measureText = measureTextUtil;
